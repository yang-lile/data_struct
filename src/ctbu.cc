#include "ctbu.h"

#include <iostream>

// template <typename T>
// class Stack : public ctbu::BaseStruct<T> {
//  private:
//   T arr[100];
//   int top;

//  public:
//   bool IsEmtry() { return this->top == 0; }

//   bool IsNotEmtry() { return this->top != 0; }

//   int GetLength() { return this->top; }

//   void push(T elem) { this->arr[this->top++] = elem; }

//   void pop() { this->top--; }

//   void Display() { printf("??\n"); }

//   void Clear() { this->top = 0; }
// };

int main(int argc, char const *argv[]) {
  // Stack<int> s;
  
  return 0;
}
